import React from 'react';
import { Container } from '../../common/components/Container';
import { ChangeContainer } from '../../common/components/ChangeContainer';

const ChangePageContainer: React.FC = () => (
  <section>
    <Container>
      <ChangeContainer />
    </Container>
  </section>
);

export default ChangePageContainer;
